#include <stdio.h>
int main()
{
int i,j,n,a[10],b[10];
clrscr();
printf("Enter the name");
scanf("%s",&a[0]);
printf("enter the second letter");
scanf("%s",&b[0]);
if(a[i]<=b[j])
printf("%s",a[0]);
else
printf("%s",b[0]);
getch();
return 0;
}
